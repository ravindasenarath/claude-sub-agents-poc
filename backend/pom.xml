<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.16</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>

    <groupId>com.plp</groupId>
    <artifactId>platform</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>platform</name>
    <description>Property Listing Platform (v1) - modular monolith backend</description>

    <properties>
        <java.version>21</java.version>
        <archunit.version>1.4.1</archunit.version>
    </properties>

    <dependencies>
        <!-- Web (REST controllers for public-api / agent-api surfaces) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Actuator: health check endpoint (ADR-0001 baseline) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!--
            Spring Security OAuth2 Resource Server: JWKS-driven bearer-token
            verification for agent-api (ADR-0002, B0.2b). Brings in
            spring-boot-starter-security (auto-secures every endpoint by
            default - see agentapi.SecurityConfig for the explicit
            permit/authenticate rules that override that default) and
            spring-security-oauth2-jose (NimbusJwtDecoder). No vendor IdP
            SDK: only standard OIDC/JWT types are used, and only inside the
            `auth` seam (module-boundaries.md rule 4;
            ModuleBoundaryTest#authLibraryTypesStayOutOfBusinessModules).
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
        </dependency>

        <!--
            Plain JDBC + HikariCP connection pool (no JPA/ORM). Entities are
            hand-written records mapped with JdbcTemplate RowMappers (see
            agent.internal); schema is versioned with Flyway (below), not an
            ORM-managed schema.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>

        <!-- PostgreSQL driver (ADR-0001: primary datastore) -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!--
            Flyway: forward-only, versioned SQL migrations under
            src/main/resources/db/migration (convention documented in
            backend/README.md). flyway-database-postgresql is required
            separately from flyway-core as of Flyway 10 for PostgreSQL
            support.
        -->
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>

        <!--
            YAML support for application.yml. Not pulled in transitively by
            the starters above in this Boot version, so it's declared
            explicitly (version managed by spring-boot-starter-parent).
        -->
        <dependency>
            <groupId>org.yaml</groupId>
            <artifactId>snakeyaml</artifactId>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- ArchUnit: mechanical enforcement of module-boundaries.md rules -->
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5</artifactId>
            <version>${archunit.version}</version>
            <scope>test</scope>
        </dependency>

        <!--
            Testcontainers: reusable Postgres harness for persistence tests
            (see AbstractPostgresIntegrationTest). Versions are managed by
            spring-boot-starter-parent's testcontainers-bom import.
        -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

</project>

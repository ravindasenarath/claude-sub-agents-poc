package com.plp.platform.agent.api;

/**
 * Marker for the {@code agent} module's published interface (see the
 * {@code agent.api} package Javadoc for the operations this will expose).
 *
 * <p>This is pure scaffolding: no methods are declared yet, and no
 * implementation exists. A follow-up task fills this in alongside the
 * {@code agent} entity/repository and the DB schema/migrations task.
 */
public interface AgentModuleApi {
}
